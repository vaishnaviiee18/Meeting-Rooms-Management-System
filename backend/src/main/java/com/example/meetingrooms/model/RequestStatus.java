package com.example.meetingrooms.model;

public enum RequestStatus {
    PENDING,   // When the booking request is pending approval
    APPROVED,  // When the booking request has been approved
    DENIED     // When the booking request has been denied
}
